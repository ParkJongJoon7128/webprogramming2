import React from "react";
import "../designs/Header.css";

const Header = () => {
  return (
    <header>
      <h1>test</h1>
      <button onClick={""}>버튼</button>
    </header>
  );
};

export default Header;
